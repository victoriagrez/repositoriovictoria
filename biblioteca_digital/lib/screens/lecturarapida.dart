import 'package:flutter/material.dart';

class LecturaRapidaScreen extends StatelessWidget {
  const LecturaRapidaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Lectura rápida')));
  }
}
